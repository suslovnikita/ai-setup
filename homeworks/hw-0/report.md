1) Добавил изначальный промпт в homeworks/hw-0/prompt.md, но хотел запускать его декомпозировано, по шагам. Запустил первый шаг с инициализацией бекенда, а в итоге он запустился целиком из файла. Тут пока не понял, как так получилось. почему-то начал записывать файлы в homeworks/hw-0, тут тоже отдельно корректировать надо было, хотя клод был запущен в корне проекта.
2) Клод сходу не нашел, как создать пользователей для бд нового проекта через суперюзера
3) новые гемы, не указанные в промпте, добавил без предупреждения (shoulda-matchers, сначала написал тесты через под него, потом добавил гем), проигнорировал CLAUDE.md
4) Проверка функционала "руками" полностью прошла успешно, при этом написанные клодом тесты падали, их правку нужно было указывать отдельно. (добавил "Не игнорировать упавшие тесты Rspec" в CLAUDE.md)
5) файлов после инициализации и первой фичи много, но в итоге все влезло в зеленый контекст до 40%. Даже не учитывая инициализацию, сама фича явно выходит за 7 файлов на сессию, но тем не менее. Интуитивно хотелось разделить, но в итоге это было как будто лишнее и лишь замедлило бы процесс, тут нужно больше практики для понимания "правила большого пальца"
